name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write
  deployments: write

jobs:
  deploy:
    uses: LABEIM/shared-ci-cd/.github/workflows/deploy-app.yml@main
    with:
      install_command: 'echo "No dependencies to install"'
      build_command: 'mkdir -p dist && cp index.html dist/'
      dist_dir: 'dist'
      enable_cloudflare: true
      cloudflare_project_name: 'comingsoon-eim'
      enable_vercel: false
      lighthouse_routes: |
        /
    secrets: inherit
